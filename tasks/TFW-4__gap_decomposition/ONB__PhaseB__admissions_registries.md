# ONB__PhaseB — Реестр ОП и Справочники (Admissions and Registries)

> **Дата**: 2026-03-07
> **Автор**: ИИ-Агент (Executor)
> **Связанная задача**: TFW-4 Phase B (Декомпозиция GAP Analysis)
> **Статус**: 🟠 ONB — Ожидает ответов

---

## 1. Анализ задачи
Изучены `HL__PhaseB` и `TS__PhaseB`. Передо мной стоит задача подготовить спецификацию `RF_TFW-4.B` для маппинга статических справочников и образовательных программ (ОП). Эта фаза критична, поскольку неправильный маппинг специальностей и ОП приведет к ошибкам валидации всех последующих данных (студентов, дипломов).

## 2. Questions (blocking — cannot proceed without answers)
| # | Question | Answer |
|---|----------|--------|
| 1 | **Справочник Специальностей:** ЕПВО требует классификацию по группам образовательных программ (ГОП). Хранит ли текущая локальная БД ВУЗа прямую привязку своих локальных специальностей к системным кодам МСКО/ОКЭД (или ID системного справочника `center_profession`), или этот маппинг придется делать через промежуточную таблицу руками/скриптом? | Хранит, но нет привязки к системным кодам МСКО/ОКЭД. Кажется в привязке нет необходимости  |
| 2 | **EducationProgram (ОП):** Для сущности `EducationProgram` в ЕПВО требуется передавать срок обучения в месяцах (`duration`). Хранится ли этот параметр в вашей БД в месяцах, в годах, или вычисляется на лету? | хранится в годах |
| 3 | **Языки обучения:** Системный справочник языков ЕПВО отличается от некоторых локальных. Например, у ЕПВО "Казахский" = 1, "Русский" = 2. Есть ли в ВУЗе прямая колонка для системных ID языков, или потребуется использовать `switch`/`case`? | Есть прямая колонка |

## 3. Recommendations (suggestions, not blocking)
1. Предлагаю для статических базовых справочников (пол, языки, формы обучения) описать в спецификации (`RF`) хардкод-перечисления (на уровне кода интеграции), так как они практически никогда не меняются, и нет смысла делать для них сложные ETL-таблицы согласования. Согласны ли вы на такой фиксаж? да

## 4. Risks Found
1. **Historical Data:** Если в ВУЗе есть старые специальности (до 2018 года), их кодировки могут не совпадать с современными кодами ГОП в ЕПВО. Попытка их загрузить вызовет ошибку 400. Скорее всего, потребуется предусмотреть `null` fallback логику для устаревших программ (если ЕПВО их принимает) или полную ручную синхронизацию старых справочников. Ответ: Где ты звял эту информацию?

## 5. Inconsistencies with Code (spec vs reality)
1. В GAP Analysis (`RF_TFW-2.3__gap_analysis.md`) упомянуто много справочников, но не все из них являются обязательными для базовой выгрузки контингента (Phase A и C). Я отфильтрую только те, что критически влияют на отправку профилей.
