# ONB__PhaseC — Академические данные (Приказы и Оценки)

> **Дата**: 2026-03-07
> **Автор**: ИИ-Агент (Executor)
> **Связанная задача**: TFW-4 Phase C (Декомпозиция GAP Analysis)
> **Статус**: 🟠 ONB — Ожидает ответов

---

## 1. Анализ задачи
Изучены `HL__PhaseC` и `TS__PhaseC`. Перед формированием итогового `RF` документа мне необходимо прояснить бизнес-требования ВУЗа относительно того, в каком виде хранятся исторические приказы по контингенту и сессионные оценки студентов.

## 2. Questions (blocking — cannot proceed without answers)
Чтобы написать корректный маппинг `RF_TFW-4.C`, мне необходимо прояснить следующие бизнес-требования ВУЗа:

| # | Question | Answer |
|---|----------|--------|
| 1 | **Транскрипты (Оценки):** Фаза C требует полноценной передачи оценок (так как это влияет на расчет стипендий и показатель СУР). В какой таблице (или представлении) вашей локальной БД лежат реальные оценки студентов за сессию, чтобы мы могли описать их маппинг в спецификации? Можете ли привести пример структуры? | Нет, можно пропустить маппинг, каждый университет имеет свою структуру БД. Опиши в общих словах на гипотетическом БД |
| 2 | **Приказы — Композитная структура:** Спецификация ЕПВО требует передавать приказ в виде вложенной структуры: `Order` -> `OrderSection` -> `SectionPerson` -> `OrderStudentInfo`. Есть ли в вашей БД все эти уровни вложенности, или приказ хранится в виде одной плоской записи (строки), которую скриптам маппинга придется "разворачивать" на 4 отдельные таблицы ЕПВО на лету? | Будем считать, что в университете приказы формируются как вложенные структуры|
| 3 | **Академическая мобильность:** Есть ли у вас процесс академической мобильности (когда студент уезжает в другой ВУЗ на семестр)? Если да, как этот статус фиксируется в базе? | Да. В таблице `order` есть поле `academic mobility` |

## 3. Risks Found
1. **Строгость Транскриптов:** Спецификация ЕПВО описывает около 12 обязательных полей для таблицы транскрипта (`type`, `accepted`, `credits`, `subjectNameRu/Kz/En`, `courseNumber`, `term`, `code` дисциплин, `isPassed`). Нам нужно убедиться, что локальная таблица оценок ВУЗа содержит все эти атрибуты, иначе API ЕПВО вернет `400 Bad Request`. Ответ: Считаем, что это соблюдено

## 4. Recommendations (suggestions)
1. Если оценки и дисциплины разбросаны по нескольким локальным таблицам (оценки, сессии, справочник дисциплин), предлагаю в итоговом `RF` описать маппинг не конкретных таблиц, а **SQL-представления (View)**, которое интегратор должен будет подготовить на стороне БД ВУЗа. Это сильно упростит работу агенту-разработчику. Ответ: Согласен
